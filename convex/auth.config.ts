const authConfig = {
    providers: [
        {
            domain: "https://stunning-midge-31.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;